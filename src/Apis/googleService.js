import { google } from "googleapis"


export const GoogleService = () => {
    const GetData = () => {
        console.log('Sheet Data loading');
    }

    const PostData = (body) => {
        console.log(body);
    }
}